package org.patterns.patterns.builder;

/**
 * What we want to create (or build) and use farther in the code
 */
public interface IUserDTO {
	
	String getName();

	String getAddress();

	String getAge();
}