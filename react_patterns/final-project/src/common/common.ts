/**
 * Basic Size type, which can be extended as needed for example with xs or xl values
 */
export type Size = 's' | 'm' | 'l';

/**
 * Enum for assembling types for specifying a position of an element
 */
export enum Position  {
    top='top',
    left='left',
    right='right',
    button='button'
};