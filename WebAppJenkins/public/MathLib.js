function sumNums(num1, num2) {
    return num1 + num2;
}

function isNegative(num) {
    return num < 0;
}

function isOdd(num) {
    return (num%2 === 0);
}

module.exports = { sumNums, isNegative, isOdd}